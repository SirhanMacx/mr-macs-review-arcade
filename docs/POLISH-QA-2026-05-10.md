# Polish Sweep QA · 2026-05-10

Generated by read-only automated sweep. No files modified.

---

## Headline numbers

| Metric | Result |
|--------|--------|
| Total game directories | 78 |
| Data-only routes (no index.html / game.js) | 20 |
| Flagship games (have index.html) | 59 (58 with game.js + boss-rush inline) |
| `node --check` game.js passing | **58 / 58** |
| HTML parse passing (hub + all flagships) | **60 / 60** |
| `games.json` entries | **220** |
| Asset modules passing `node --check` | **28 / 28** |
| Bug-pattern hits (`MrMacsToast.show`) | **0** |
| Bug-pattern hits (`MrMacsToasts` plural) | **0** (1 hit in changelog text — safe) |
| Bug-pattern hits (bare `MrMacsAnalytics.track`) | **0** (2 hits are guarded fallback patterns) |
| Flagship games missing "Back to Arcade" nav | **2** confirmed (`boss-rush`, `regents-gauntlet`) |

---

## 1. Module dependency fix verification

**Rec: leaderboards + sessions must load before progress-extras.**

Hub `index.html` lines 8560–8568:

```html
<!-- arcade-leaderboards.js + arcade-sessions.js MUST load BEFORE
     arcade-progress-extras.js. Progress-extras tries to grab all three
     … -->
<script src="assets/arcade-leaderboards.js?v=20260510-leaderboards"></script>
<script src="assets/arcade-sessions.js?v=20260510-sessions"></script>
<script src="assets/arcade-progress-extras.js?v=20260510-progress-extras"></script>
```

**VERIFIED** — corrected load order is in place. Comment documents the dependency.

---

## 2. UX walkthrough fixes verification

### Rec #1 — Collapsed "What's new" callouts (deduplication)

- `id="whatsNewCallout"` appears **exactly once** in hub DOM (line 8275).
- The `<section class="band round3-callout-band">` element has been **removed**; a comment at line 8421 confirms it: `<!-- Round 3 callout removed (was redundant with #whatsNewCallout above…) -->`.
- CSS rules for `.round3-callout-band` remain in the stylesheet (dead but harmless; they affect no DOM element).

**VERIFIED — only 1 callout band in DOM.**

### Rec #2 — Daily Challenge CTA "+60 💎" payout chip

Hub `index.html` line 8182:

```html
<span class="daily-cta-payout" aria-label="60 shard payout">+60 💎</span>
```

Also confirmed at line 8118: `<button … id="dailyPlanClaim" … >Claim +60 shards 💎</button>`

**VERIFIED — payout chip present.**

### Rec #3 — Library "Newest" sort option

Hub `index.html` line 8480:

```html
<option value="newest">Sort: Newest</option>
```

Sort handler at line 9013 confirms `state.sort === "newest"` branch is implemented.

**VERIFIED — Newest sort option in DOM and wired up.**

### Rec #4 — `arcade-toast.js` pointer-events: none on container

`assets/arcade-toast.js` lines 115–132:

```css
.arcade-toast-container {
  …
  pointer-events: none;    /* container transparent to clicks */
}
.arcade-toast {
  pointer-events: auto;    /* individual toasts still clickable */
  …
}
```

**VERIFIED — container blocks no clicks, individual toasts remain interactive.**

### Rec #5 — MrMacsToast API consistency

- `MrMacsToast.show` typo: **0 hits** across all game JS.
- `MrMacsToasts` plural typo: **0 live hits** (1 hit is inside a changelog description string — not executable code).
- All game usage calls `MrMacsToast.push(…)` or `MrMacsToast.shards(…)`, matching the module's public API.

**VERIFIED — API is consistent.**

### Rec #6 — Deprecated `MrMacsAnalytics.track`

Two hits found; both are guarded fallback patterns:

| Location | Pattern |
|----------|---------|
| `assets/mastery-engine.js:441` | `if (window.MrMacsAnalytics && window.MrMacsAnalytics.track)` |
| `assets/arcade-changelog.js:513` | Inside a changelog string literal (non-executable) |

Zero bare unguarded `MrMacsAnalytics.track(…)` calls in production code paths.

**VERIFIED — no deprecated bare analytics calls.**

---

## 3. Console statement audit

All `console.error` calls found in games are in legitimate error-handling paths (catch blocks or `.catch()` chains). No debug `console.log` or stray `console.warn` in game logic:

| Game | Location | Context |
|------|----------|---------|
| arcade-duel | game.js:1312 | `try/catch` bank load |
| archive-cipher | game.js:790 | `.catch(error =>` |
| archive-quest | game.js:1263 | `.catch(error =>` |
| chrono-defense | game.js:2872 | `init().catch(err =>` |
| chrono-pinball | game.js:2796 | Bank load `.catch` |
| empire-ascendant | game.js:2876 | Bank load `.catch` |
| history-hunters | game.js:3465 | `.catch(error =>` |
| lightning-review | game.js:36 | `fetch(…).catch(err =>` |
| mastery-path | game.js:219 | `boot().catch(error =>` |
| source-audit | game.js:265 | `.catch(error =>` |
| source-lab | game.js:201 | `boot().catch(error =>` |
| source-sprint | game.js:42 | `fetch(…).catch(err =>` |
| time-rift-survivors | game.js:2974 | `init().catch(error =>` |
| timeline-runner | game.js:2991 | `.catch(err =>` |
| vocab-vault | game.js:36 | `fetch(…).catch(err =>` |
| writing-coach | game.js:316 | `boot().catch(error =>` |

Asset module consoles (`arcade-analytics.js`, `arcade-deeplinks.js`, `arcade-game-helpers.js`, `arcade-music.js`, `arcade-profile.js`, `arcade-screenshot.js`) are all behind `debugMode` guards, `typeof console` checks, or `root.console` conditional access — appropriate for production library code.

**No stray debug console.log statements found.**

---

## 4. Per-game status

### Flagship games (59 with index.html)

| Game | game.js parses | index.html parses | Has back-to-arcade nav |
|------|---------------|------------------|-----------------------|
| anagram-atlas | OK | OK | YES (HTML link) |
| ap-practice-exam | OK | OK | YES |
| arcade-duel | OK | OK | YES (HTML link) |
| archive-cipher | OK | OK | YES (JS: backBtn → index.html) |
| archive-quest | OK | OK | YES (HTML link) |
| archive-tycoon | OK | OK | YES |
| atlas-2048 | OK | OK | YES |
| boggle-beat | OK | OK | YES |
| bomb-scribe | OK | OK | YES |
| boss-rush | N/A (inline) | OK | **NO** — no `../../index.html` reference found |
| brickoria | OK | OK | YES |
| cascade | OK | OK | YES |
| centiquill | OK | OK | YES |
| chess-cabinet | OK | OK | YES |
| chrono-defense | OK | OK | YES (JS: runQuit → index.html) |
| chrono-pinball | OK | OK | YES |
| chronoblocks | OK | OK | YES |
| chronohop | OK | OK | YES |
| citadel | OK | OK | YES |
| cold-war-invaders | OK | OK | YES |
| crossword-cabinet | OK | OK | YES |
| cube-crash | OK | OK | YES |
| defender-drift | OK | OK | YES |
| echo-hall | OK | OK | YES |
| empire-ascendant | OK | OK | YES (arcade-home-mark link) |
| galaxy-defender | OK | OK | YES |
| history-hunters | OK | OK | YES |
| history-hunters-2 | OK | OK | YES (JS nav) |
| knights-quest | OK | OK | YES |
| lightning-review | OK | OK | YES |
| mahjong-mosaic | OK | OK | YES |
| mastery-path | OK | OK | YES (HTML link) |
| memory-palace | OK | OK | YES |
| plinko-lab | OK | OK | YES |
| pong-doctor | OK | OK | YES |
| reflex-run | OK | OK | YES |
| regents-gauntlet | OK | OK | **NO** — no arcade nav in index.html or game.js |
| regents-practice-exam | OK | OK | YES |
| regents-rally-source-circuit | OK | OK | YES |
| review-maze-chase | OK | OK | YES |
| rumor-whack | OK | OK | YES |
| snake-pit | OK | OK | YES |
| sokoban-scribe | OK | OK | YES |
| solitaire-hall | OK | OK | YES |
| source-audit | OK | OK | YES (HTML link) |
| source-lab | OK | OK | YES (HTML link) |
| source-snake | OK | OK | YES |
| source-sprint | OK | OK | YES |
| stellar-drift | OK | OK | YES |
| step-pyramid | OK | OK | YES |
| sudoku-scribe | OK | OK | YES |
| time-rift-survivors | OK | OK | YES (JS recordPlay) |
| timeline-runner | OK | OK | YES (backBtn → exit to arcade) |
| tower-climb | OK | OK | YES |
| tron-trails | OK | OK | YES |
| vocab-vault | OK | OK | YES |
| word-bridge | OK | OK | YES |
| writing-coach | OK | OK | YES (HTML link) |

### Data-only routes (20, no index.html — not applicable to back-link check)

`ap-economics-combined`, `ap-european-history`, `ap-human-geography`, `ap-macroeconomics`, `ap-microeconomics`, `ap-psychology`, `ap-us-government`, `ap-world-history`, `apush`, `civics-pig`, `economics`, `global-10-units`, `global-9`, `global-regents-sprint`, `grade-5`, `grade-6`, `grade-7`, `grade-8`, `us-history-units`, `us-regents-sprint`

---

## 5. Remaining issues

### P1 — `boss-rush` missing arcade navigation

**File**: `games/boss-rush/index.html`
**Issue**: The Boss Rush Arena game (2533-line inline app) has no link or button that navigates back to `../../index.html`. Players who enter this game cannot exit to the hub without using the browser back button.
**Fix**: Add a `<a class="back-link" href="../../index.html" aria-label="Back to arcade hub">← Arcade</a>` in the header nav (around line 654 near `.brand`).

### P1 — `regents-gauntlet` missing arcade navigation

**File**: `games/regents-gauntlet/index.html`, `games/regents-gauntlet/game.js`
**Issue**: Neither file contains any reference to `../../index.html`, `arcadeHome`, or any exit-to-arcade pattern. The game's end-of-run buttons (`Run It Back`, `Change Mission`) both stay within the game. No escape route to hub.
**Fix**: Add back-link in the `.brand` nav div at line 18 of `index.html`:
```html
<a class="back-link" href="../../index.html" aria-label="Back to arcade hub">← Arcade</a>
```

### P2 — Dead CSS for `.round3-callout-band`

**File**: `index.html` lines 6283–6369
**Issue**: ~80 lines of CSS for `.round3-callout-band`, `.round3-callout-card`, `.round3-callout-copy`, etc. remain in the stylesheet after the corresponding DOM element was removed. Dead code only — no functional impact.
**Fix**: Remove the CSS block at lines 6283–6369 in a future cleanup pass.

### P3 — `console.error` in production catch paths (minor / acceptable)

13 game files + 3 asset modules contain `console.error` inside catch blocks. This is conventional JavaScript error logging and appropriate in production. However, for a hardened release these could be replaced with silent no-ops or structured error reporting via `MrMacsAnalytics`. Low priority.

### P3 — `regents-gauntlet` has no `game.js`; all logic is inline in `index.html`

This is a structural inconsistency vs. every other game. Not a functional bug, but makes the game harder to maintain and node-check (syntax was validated only via html.parser which does not check inline `<script>` blocks).

---

## 6. Recommendations (priority order)

1. **Add back-link to `boss-rush/index.html`** (P1) — one line near the `.brand` header block.
2. **Add back-link to `regents-gauntlet/index.html`** (P1) — one line near the `.brand` div.
3. **Remove dead `.round3-callout-band` CSS** from hub `index.html` (P2) — ~80 lines, zero risk.
4. **Extract `regents-gauntlet` inline JS to `game.js`** (P3) — improves maintainability and enables `node --check` validation.
5. **Sweep `console.error` to silent handlers** in catch paths (P3) — optional hardening for final production build.

---

## 7. Summary

- **All 28 asset modules pass `node --check`.** Zero syntax errors.
- **All 58 game.js files pass `node --check`.** Zero syntax errors.
- **All 60 HTML files (hub + 59 flagships) parse cleanly.** Zero structural errors.
- **`games.json` parses cleanly** with 220 entries.
- **Zero live bug-pattern hits**: no `MrMacsToast.show` typo, no `MrMacsToasts` plural, no bare `MrMacsAnalytics.track`.
- **All 5 session-specific polish fixes verified**: script-tag load order correct, single callout band in DOM, +60 💎 chip present, Newest sort option present, pointer-events fix in arcade-toast.js confirmed.
- **57 of 59 flagship games** have a clear path back to the arcade hub. Two (`boss-rush`, `regents-gauntlet`) are missing arcade navigation and should be patched before the next public release.
